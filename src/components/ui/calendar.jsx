// Calendar component removed along with react-day-picker dependency to
// resolve React 19 peer dependency conflicts. This file is kept as a stub
// to avoid broken imports if any stale code references it. If you need a
// date picker again, prefer a lightweight wrapper around <input type="date">
// or add a React 19 compatible calendar library.

export const Calendar = () => null;
