# Example local env overrides for dev server
# Copy to .env.local and adjust as needed

# Strict dev port (Vite)
VITE_PORT=5173
# Host binding (0.0.0.0 to allow LAN access; default is 0.0.0.0 in config)
VITE_HOST=0.0.0.0
